# .semaphore/semaphore.yml

version: v1.0
name: Your Node.js Project CI/CD Pipeline

agent:
  machine:
    type: e1-standard-2

blocks:
  - name: Build and Test
    task:
      jobs:
        - name: Install Dependencies
          commands:
            - npm install
        - name: Run Tests
          commands:
            - npm test